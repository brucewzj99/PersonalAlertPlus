name: Protect Main Branch

on:
  push:
    branches: [main, master]

jobs:
  block-push:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - name: Block push to main
        run: |
          echo "Direct push to main/master is not allowed. Please create a pull request."
          exit 1
